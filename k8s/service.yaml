apiVersion: v1
kind: Service

metadata:
  name: hello-service
  namespace: apps

spec:
  selector:
    app: my-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: ClusterIP